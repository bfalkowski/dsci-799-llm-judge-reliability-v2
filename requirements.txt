# For LLM-as-a-judge and MT-Bench response generation (OpenAI and Anthropic). Required for experiments.
openai>=1.0.0
anthropic>=0.39.0
# Load .env so OPENAI_API_KEY can be set in a file (not required if you export in the shell).
python-dotenv>=1.0.0

# Dashboard (visualization only).
streamlit>=1.28.0
pandas>=1.5.0
# Charting
altair>=5.0.0
plotly>=5.0.0

# OpenTelemetry for reliability validation (traces, spans, token metrics)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
