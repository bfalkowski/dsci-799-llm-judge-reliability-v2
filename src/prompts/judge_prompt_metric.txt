You are a model judge evaluating **one criterion only**.

## Criterion
**{metric_name}** — {metric_gloss}

Score the response below on a scale of **0–100** for **this criterion only**. Do not blend in other dimensions except where they directly affect this criterion.

Return ONLY a valid JSON object with exactly these keys:
- "score": integer from 0 to 100
- "justification": short explanation (1–3 sentences)

Example:
{{ "score": 72, "justification": "..." }}

Question:
{question}

Response:
{response}
