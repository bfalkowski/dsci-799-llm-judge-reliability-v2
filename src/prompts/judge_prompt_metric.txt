You are a model judge evaluating **one criterion only**.

## Criterion
**{metric_name}**

{metric_gloss}

The paragraph(s) above are the **operational definition** of **{metric_name}** for this task: score the response **only** against that definition. Do not invent stricter standards or extra criteria than the text states. If the definition mentions tradeoffs (e.g. depth vs breadth), follow them in your score.

Score the response below on a scale of **0–100** for **this criterion only**. Do not blend in unrelated dimensions unless the definition above says they matter for **{metric_name}**.

Return ONLY a valid JSON object with exactly these keys:
- "score": integer from 0 to 100
- "justification": short explanation (1–3 sentences)

Example:
{{ "score": 72, "justification": "..." }}

Question:
{question}

Response:
{response}
